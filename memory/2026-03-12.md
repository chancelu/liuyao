# 2026-03-12 Daily Notes

## liuyao-web 音效反馈

- 小北反馈：当前音效"还是不像铜钱碰撞"，希望是**一声清脆的金属碰撞声**，而不是铃铛/叮铃铃感
- 已修改 `src/lib/audio/coin-sound.ts`
- 调整方向：
  - 去掉原本偏铃声、偏尾音的连续金属泛音
  - 改成更短、更硬、更亮的瞬态撞击
  - 增加极轻微的二次碰撞，模拟铜钱落下后轻磕一下
- 本地 build 通过
- 已提交 commit: `ce68046` (`Refine coin collision sound`)

## 记忆要求

- 小北明确要求：**把内容都保存进记忆**
- 后续对话里的重要信息、偏好、决策、项目变更，需要主动写入 daily memory；长期稳定偏好与工作习惯要同步沉淀到 `MEMORY.md`

## Product Manager Skill 安装

- 安装了 `1kalin/afrexai-product-manager` skill 到 `~/.openclaw/skills/product-manager/`
- 包含：SKILL.md, README.md, _meta.json
- 功能：产品策略、用户调研、RICE 优先级排序、roadmap、PRD/spec、launch checklist、metrics
- 给小北整理了中文使用速查指南

## OpenClaw Watchdog 开机自启

- 创建了 watchdog 脚本（`scripts/openclaw-watchdog.ps1`, `scripts/openclaw-recover.ps1`）
- 已 commit: `f82256a`
- 注册计划任务被拒（需管理员权限），改用 Startup 文件夹方案：
  - `C:\Users\llc\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\openclaw-watchdog-startup.cmd`
  - 登录后自动最小化启动 watchdog
- 也给小北准备了管理员 PowerShell 命令（Register-ScheduledTask），他说晚点处理

## 古铜钱音效 v2 重写

- 小北要求把音效改成**古铜钱碰撞声**（不是铃铛/叮铃声）
- 完全重写了 `coin-sound.ts`，新方案：
  - 三枚铜钱快速碰撞，每枚音高不同（1150Hz / 1340Hz / 960Hz）
  - 每次碰撞叠 4 层：noise burst + triangle body + sine sub-harmonic + square edge
  - 加入 jitter 随机性（频率±4%、音量±8%、时间±10-12%）
  - 频率从 4100-6100Hz 降到 960-1340Hz，更厚重
- commit: `2ef9b31`

## GitHub Push 被拦截（Secret Scanning）

- push 到 `chancelu/liuyao` 被 GitHub Push Protection 拦住
- 原因：`memory/2026-03-11.md` 第 42 行包含 Vercel Personal Access Token
  - token: [REDACTED]
  - 出现在 commit `c751ddb` 和 `c38d4bf`
- 尝试用 `git filter-branch` 清除但失败（文件锁 + 进程被 kill）
- 尝试安装 `git-filter-repo`（pip install 成功但 git 找不到命令）
- **待解决**：需要从 git 历史中彻底删除 `memory/2026-03-11.md` 和 `self_monitor/logs/guardian.log`
- 可选方案：
  1. 修复 git-filter-repo 路径问题后重试
  2. 用 GitHub 提供的 unblock URL 临时放行
  3. 用 interactive rebase 手动编辑那两个 commit
- git 仓库根目录是 `D:/openclaw/workspace`（不是 liuyao-web 子目录）
