# 2026-03-08

- 小北发来《六爻习题卷（完整版）》PDF，让我验证 `liuyao-calculation` skill 是否正确、好用。
- 已自行安装并使用 `rapidocr_onnxruntime` 对 PDF 做 OCR，成功抽取后段题目与答案卷内容。
- 初步验证结果：skill 骨架正确，适合取用神/旺衰题；象法、应期仍需补强。
- 已升级 `skills/liuyao-calculation/`，新增：
  - `references/answer-calibration.md`
  - `references/xiangfa-patterns.md`
  - `references/timing-patterns.md`
  - `references/timing-priority-rules.md`
  - `references/validation-2026-03-08.md`
- 升级后重新回测，当前主观评分提升到约 8/10；应期规则比第一版清楚很多。
- 基于六爻能力规划了一个在线网站 MVP：产品方案、PRD、视觉方向、页面结构、技术架构、开发里程碑、Backlog、数据库/API 草案、页面状态流均已整理完成。
- 已正式创建项目 `liuyao-web/`，技术栈为 Next.js + TypeScript + Tailwind；预埋中文优先的 i18n 结构，并搭出首页、起卦页、摇卦页、生成页、结果页、历史页、分享页的第一版页面骨架。
- `liuyao-web` 已通过 `npm run lint` 与 `npm run build`。
- 继续开发 `liuyao-web`，已把主流程从静态骨架推进到可交互 mock：提问 → 摇卦 → 生成 → 结果。
- 新增共享类型与 mock 结果生成逻辑，建立起 draft / cast / result 的前端状态流。
- 结果页已能按当前 divination id 读取本地 mock 结果，不再是纯静态示意。
- 为解决开发中出现的问题，修复了一个 React effect 中同步 `setState` 的 lint 报错，以及 Next.js 对 `useSearchParams()` 缺少 Suspense 边界导致的 build 失败；修复后再次通过 `npm run lint` 与 `npm run build`。
- 本轮代码已提交 git：`Add interactive MVP casting flow mock`。
- 随后继续往正式架构推进：新增 `.env.example`、环境变量读取层、Supabase browser client 骨架、本地 storage 层、`divination-service` 服务层，以及 `/api/health` 健康检查接口。
- 已加上 guest session / free trial 的本地结构位，当前游客体验次数限制逻辑先在前端骨架中占位，后续再接入真实登录与后端存储。
- README 已更新，说明当前阶段、环境变量和下一步计划。
- 本轮代码已提交 git：`Scaffold supabase and data flow foundations`。
- 小北注意到一次 `ask-form.tsx` 的工具编辑失败提示；已确认那只是一次精确替换未命中，不是代码损坏，当前文件与整个项目状态正常，且构建通过。

- 小北明确提出一个协作偏好：有进展时要主动通知，不要总等他来追问进度；后续开发过程中，完成阶段性成果、出现明确卡点、或需要提供配置/信息时，应主动发消息同步。

- 小北再次强调协作规则：凡是出现卡住、死循环、外部依赖无法自行解决、或进度明显停滞时，要主动立刻找他，不要空耗时间。
