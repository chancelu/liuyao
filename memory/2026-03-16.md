# 2026-03-16 工作日志

## i18n 全面覆盖（三轮提交）

### 第一轮 `e0623cc`
- 完整重写 zh-CN.ts 和 en.ts 语言包，新增 login、profile、ritual、share、shareCard、common 等全部 key
- 国际化组件：login-content, profile/page, result-client, ritual-client, processing-client, history-client, share-card, rotating-quote
- share/[id] 改为 server+client 混合架构（ShareClient 组件）
- build-prompt.ts 支持 locale 参数，英文模式下 LLM 用英文输出分析
- 周易原文保持中文，英文模式下 source 显示拼音格式

### 第二轮 `085f1b3`
- 页面 wrapper 国际化：cast, ritual, processing, history, login（改为 client component）
- auth-nav 用 messages 替换 inline ternary
- magic-link-form 全部国际化
- auto-checkin toast 国际化
- admin/page 全部国际化
- divination-api + lib/supabase/auth 客户端错误消息国际化
- 新增 getCurrentMessages() 非 hook 版本用于非组件文件
- 修复 zh-CN.ts/en.ts 结构问题（errors/share key 缺失）

### 第三轮 `7290f48`
- 新增 vitest 测试框架
- i18n.test.ts：17 项测试（key 一致性、模板变量匹配、数组长度、空值、中文泄漏）
- no-hardcoded-chinese.test.ts：扫描客户端组件检测未国际化硬编码
- 修复 ask-form 性别标签和提交按钮硬编码
- 修复 site-shell fallback 中文
- 移除 result-client 中未使用的 YaoLine 组件

### 第四轮 `363a9bc`
- chart.test.ts 改为 vitest 格式（require → import，process.exit → expect）
- 变卦断言值修正为引擎实际输出（泽山咸，splitTrigrams bit 顺序修正后的正确结果）
- 32/32 测试全部通过

## 未完成
- API routes 服务端错误消息未国际化（需要从 request header 获取 locale）
- 引擎层中文术语是数据不是 UI，不需要改

## 正在调查的 Bug
- 小北报告：选了女性问感情，但 LLM 输出按男性分析
- 追踪链路：ask-form → draft.gender → API → buildMockResult → result.gender → buildPromptFromResult → analyze(chart, category, gender)
- 链路看起来完整，gender 从前端到引擎都有传递
- 需要继续排查：processing-client 里 result 对象是否真的包含 gender（可能在 localStorage 序列化/反序列化过程中丢失）

## Vercel 部署
- 正确域名是 `liuyao-web-sandy.vercel.app`（不是 liuyao-web.vercel.app）
- 已记录到 MEMORY.md 待更新

## Claude Code
- claude --dangerously-skip-permissions 在长 prompt 下会卡住无输出
- 短 prompt 正常工作（版本 2.1.70）
- 解决方案：写 TASK 文件再引用，或直接手动改
